import PageTransition from "@/components/global/page-transition";
import React from "react";

type Props = {};

const AssetClasses = (props: Props) => {
  return (
    <PageTransition>
      <div>AssetClasses</div>
    </PageTransition>
  );
};

export default AssetClasses;
